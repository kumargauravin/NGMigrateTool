import { LimitedPathPipe } from './limited-path.pipe';

describe('LimitedPathPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitedPathPipe();
    expect(pipe).toBeTruthy();
  });
});
